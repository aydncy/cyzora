'use client';

export default function Home() {
  return (
    <>
      <nav style={{
        position: 'fixed',
        top: 0,
        left: 0,
        right: 0,
        zIndex: 50,
        borderBottom: '1px solid rgba(255,255,255,0.05)',
        backdropFilter: 'blur(10px)',
        background: 'rgba(3, 7, 18, 0.8)',
      }}>
        <div style={{
          maxWidth: '1200px',
          margin: '0 auto',
          padding: '16px 24px',
          display: 'flex',
          justifyContent: 'space-between',
          alignItems: 'center',
        }}>
          <div style={{
            fontSize: '20px',
            fontWeight: '800',
            background: 'linear-gradient(135deg, #3b82f6, #06b6d4)',
            WebkitBackgroundClip: 'text',
            WebkitTextFillColor: 'transparent',
            backgroundClip: 'text',
          }}>
            Cyzora
          </div>

          <div style={{ display: 'flex', gap: '32px', alignItems: 'center' }}>
            <a href="#features" style={{ color: 'rgba(203, 213, 225, 0.8)', fontSize: '14px', fontWeight: '500', cursor: 'pointer' }}>Features</a>
            <a href="#pricing" style={{ color: 'rgba(203, 213, 225, 0.8)', fontSize: '14px', fontWeight: '500', cursor: 'pointer' }}>Pricing</a>
            <a href="#docs" style={{ color: 'rgba(203, 213, 225, 0.8)', fontSize: '14px', fontWeight: '500', cursor: 'pointer' }}>Docs</a>
            <button className="btn primary" style={{ padding: '10px 20px', fontSize: '14px' }}>Get Started</button>
          </div>
        </div>
      </nav>

      <main className="page" style={{ paddingTop: '140px' }}>
        <section style={{
          display: 'grid',
          gridTemplateColumns: '1.2fr 0.8fr',
          gap: '60px',
          alignItems: 'center',
          marginBottom: '100px',
        }}>
          <div>
            <span className="badge" style={{ display: 'inline-flex', marginBottom: '24px' }}>✨ Now available</span>

            <h1 style={{ marginTop: '16px', marginBottom: '24px' }}>
              Stop debugging.
              <br />
              Start <span className="gradient-text">verifying.</span>
            </h1>

            <p style={{
              fontSize: '18px',
              color: 'rgba(203, 213, 225, 0.9)',
              maxWidth: '500px',
              lineHeight: '1.8',
              marginBottom: '32px',
            }}>
              Verify webhooks instantly with real-time feedback and zero setup. Built for developers who value their time.
            </p>

            <div style={{ display: 'flex', gap: '16px', marginBottom: '48px' }}>
              <button className="btn primary" style={{ cursor: 'pointer' }}>Try free →</button>
              <button className="btn secondary" style={{ cursor: 'pointer' }}>View docs</button>
            </div>

            <div style={{
              display: 'flex',
              alignItems: 'center',
              gap: '32px',
              borderTop: '1px solid rgba(255,255,255,0.1)',
              paddingTop: '32px',
            }}>
              <div>
                <div style={{ fontSize: '24px', fontWeight: '800', color: '#3b82f6' }}>50K+</div>
                <div style={{ fontSize: '13px', color: 'rgba(148, 163, 184, 1)', marginTop: '4px' }}>Developers trusted</div>
              </div>
              <div>
                <div style={{ fontSize: '24px', fontWeight: '800', color: '#10b981' }}>99.9%</div>
                <div style={{ fontSize: '13px', color: 'rgba(148, 163, 184, 1)', marginTop: '4px' }}>Uptime SLA</div>
              </div>
              <div>
                <div style={{ fontSize: '24px', fontWeight: '800', color: '#f59e0b' }}>&lt;100ms</div>
                <div style={{ fontSize: '13px', color: 'rgba(148, 163, 184, 1)', marginTop: '4px' }}>Response time</div>
              </div>
            </div>
          </div>

          <div className="panel accent" style={{
            cursor: 'pointer',
            transition: 'all 300ms',
            transform: 'scale(1)',
          }} onMouseEnter={(e) => {
            e.currentTarget.style.transform = 'scale(1.02)';
            e.currentTarget.style.borderColor = '#3b82f6';
          }} onMouseLeave={(e) => {
            e.currentTarget.style.transform = 'scale(1)';
            e.currentTarget.style.borderColor = 'rgba(59, 130, 246, 0.3)';
          }}>
            <div style={{
              borderBottom: '1px solid rgba(255,255,255,0.1)',
              paddingBottom: '12px',
              marginBottom: '16px',
              display: 'flex',
              justifyContent: 'space-between',
              alignItems: 'center',
            }}>
              <div style={{ fontSize: '12px', color: 'rgba(148, 163, 184, 1)', fontWeight: '700', letterSpacing: '0.1em' }}>
                WEBHOOK / VERIFICATION
              </div>
              <div style={{
                width: '8px',
                height: '8px',
                borderRadius: '50%',
                background: '#10b981',
                boxShadow: '0 0 10px rgba(16, 185, 129, 0.5)',
              }} />
            </div>

            <div style={{
              background: 'rgba(15, 23, 42, 0.8)',
              border: '1px solid rgba(71, 85, 105, 0.3)',
              borderRadius: '8px',
              padding: '16px',
              fontFamily: 'Fira Code, monospace',
              fontSize: '13px',
              color: '#e0e7ff',
              marginBottom: '20px',
            }}>
              <div style={{ color: '#94a3b8', marginBottom: '8px' }}>POST /verify</div>
              <div style={{ color: '#cbd5e1', marginBottom: '4px' }}>provider: <span style={{ color: '#34d399' }}>stripe</span></div>
              <div style={{ color: '#cbd5e1' }}>timestamp: <span style={{ color: '#f59e0b' }}>1234567890</span></div>
            </div>

            <button className="btn primary" style={{ width: '100%', marginBottom: '20px', justifyContent: 'center' }}>
              Verify Webhook
            </button>

            <div style={{
              background: 'rgba(16, 185, 129, 0.1)',
              border: '1px solid rgba(16, 185, 129, 0.3)',
              borderRadius: '8px',
              padding: '12px',
            }}>
              <div style={{ color: '#34d399', fontWeight: '600', fontSize: '14px', display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '8px' }}>
                <span>✓</span> VALID WEBHOOK
              </div>
              <div style={{ color: 'rgba(148, 163, 184, 1)', fontSize: '13px', marginBottom: '4px' }}>Signature verified</div>
              <div style={{ color: '#f59e0b', fontSize: '13px', fontWeight: '500' }}>latency: 24ms</div>
            </div>
          </div>
        </section>

        <section id="features" style={{ marginBottom: '100px' }}>
          <div style={{ textAlign: 'center', marginBottom: '60px' }}>
            <h2>Why developers choose Cyzora</h2>
            <p style={{ marginTop: '16px', maxWidth: '600px', margin: '16px auto 0' }}>
              Built by developers for developers. Simple, powerful, and blazingly fast.
            </p>
          </div>

          <div style={{
            display: 'grid',
            gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))',
            gap: '24px',
          }}>
            {[
              { icon: '⚡', title: 'Real-time Verification', desc: 'Instantly verify webhook signatures.' },
              { icon: '🔒', title: 'Enterprise Security', desc: 'Bank-grade encryption for webhooks.' },
              { icon: '📊', title: 'Complete Analytics', desc: 'Track webhook performance.' },
              { icon: '🚀', title: 'Zero Setup', desc: 'No configuration needed.' },
              { icon: '🌐', title: 'Global Infrastructure', desc: 'Distributed globally.' },
              { icon: '💼', title: 'Developer Experience', desc: 'Excellent docs and support.' },
            ].map((feature, idx) => (
              <div key={idx} className="panel" style={{ cursor: 'pointer' }} onMouseEnter={(e) => e.currentTarget.style.transform = 'translateY(-4px)'} onMouseLeave={(e) => e.currentTarget.style.transform = 'translateY(0)'}>
                <div style={{ fontSize: '32px', marginBottom: '12px' }}>{feature.icon}</div>
                <h4 style={{ marginBottom: '8px' }}>{feature.title}</h4>
                <p style={{ fontSize: '14px', color: 'rgba(148, 163, 184, 1)' }}>{feature.desc}</p>
              </div>
            ))}
          </div>
        </section>

        <section id="pricing" style={{ marginBottom: '100px' }}>
          <div style={{ textAlign: 'center', marginBottom: '60px' }}>
            <span className="badge">💰 Simple Pricing</span>
            <h2 style={{ marginTop: '16px' }}>Plans for every team</h2>
            <p style={{ marginTop: '16px', maxWidth: '600px', margin: '16px auto 0' }}>
              Start free. Scale as you grow. No credit card required.
            </p>
          </div>

          <div style={{
            display: 'grid',
            gridTemplateColumns: 'repeat(auto-fit, minmax(300px, 1fr))',
            gap: '24px',
            maxWidth: '1000px',
            margin: '0 auto',
          }}>
            {[
              {
                name: 'Starter',
                price: '$0',
                period: 'Forever free',
                features: ['1,000 verifications/month', 'Basic analytics', 'Community support', 'No credit card'],
                highlight: false,
              },
              {
                name: 'Professional',
                price: '$49',
                period: 'Per month',
                features: ['100K verifications/month', 'Advanced analytics', 'Priority support', 'Webhook retries', 'Custom domain'],
                highlight: true,
              },
              {
                name: 'Enterprise',
                price: 'Custom',
                period: 'Contact sales',
                features: ['Unlimited verifications', 'Dedicated support', 'SLA guarantee', 'Custom integration', 'On-premise option'],
                highlight: false,
              },
            ].map((plan, idx) => (
              <div key={idx} className={`panel ${plan.highlight ? 'accent' : ''}`} style={{
                border: plan.highlight ? '2px solid #3b82f6' : '1px solid rgba(226, 232, 240, 0.1)',
                transform: plan.highlight ? 'scale(1.05)' : 'scale(1)',
              }} onMouseEnter={(e) => e.currentTarget.style.transform = plan.highlight ? 'scale(1.08)' : 'translateY(-4px)'} onMouseLeave={(e) => e.currentTarget.style.transform = plan.highlight ? 'scale(1.05)' : 'translateY(0)'}>
                {plan.highlight && <span className="badge" style={{ marginBottom: '16px' }}>Popular</span>}
                <h4 style={{ marginBottom: '8px' }}>{plan.name}</h4>
                <div style={{ marginBottom: '16px' }}>
                  <div style={{ fontSize: '32px', fontWeight: '800', color: plan.highlight ? '#3b82f6' : '#f3f6fb' }}>{plan.price}</div>
                  <div style={{ fontSize: '13px', color: 'rgba(148, 163, 184, 1)' }}>{plan.period}</div>
                </div>
                <ul style={{ listStyle: 'none', marginBottom: '24px' }}>
                  {plan.features.map((feature, fidx) => (
                    <li key={fidx} style={{ fontSize: '14px', color: 'rgba(203, 213, 225, 0.9)', marginBottom: '12px', display: 'flex', alignItems: 'center', gap: '8px' }}>
                      <span style={{ color: '#10b981' }}>✓</span>
                      {feature}
                    </li>
                  ))}
                </ul>
                <button className={`btn ${plan.highlight ? 'primary' : 'secondary'}`} style={{ width: '100%' }}>
                  {plan.highlight ? 'Start free trial' : 'Get started'}
                </button>
              </div>
            ))}
          </div>
        </section>

        <section style={{
          background: 'linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(6, 182, 212, 0.05))',
          border: '1px solid rgba(59, 130, 246, 0.2)',
          borderRadius: '20px',
          padding: '60px 40px',
          textAlign: 'center',
          marginBottom: '100px',
        }}>
          <h2>Ready to start verifying?</h2>
          <p style={{ marginTop: '16px', marginBottom: '32px', maxWidth: '600px', margin: '16px auto 32px' }}>
            Join 50,000+ developers who trust Cyzora for webhook verification.
          </p>
          <button className="btn primary">Get started free →</button>
        </section>
      </main>

      <footer style={{
        borderTop: '1px solid rgba(255,255,255,0.05)',
        padding: '60px 24px',
        background: 'rgba(3, 7, 18, 0.5)',
      }}>
        <div style={{ maxWidth: '1200px', margin: '0 auto' }}>
          <div style={{
            display: 'grid',
            gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))',
            gap: '40px',
            marginBottom: '40px',
          }}>
            <div>
              <h5 style={{ marginBottom: '16px' }}>Product</h5>
              <ul style={{ listStyle: 'none' }}>
                {['Features', 'Pricing', 'Docs', 'API', 'Status'].map((item) => (
                  <li key={item} style={{ marginBottom: '8px' }}>
                    <a href="#" style={{ color: 'rgba(148, 163, 184, 1)', fontSize: '14px' }}>{item}</a>
                  </li>
                ))}
              </ul>
            </div>
            <div>
              <h5 style={{ marginBottom: '16px' }}>Company</h5>
              <ul style={{ listStyle: 'none' }}>
                {['About', 'Blog', 'GitHub', 'Twitter', 'Contact'].map((item) => (
                  <li key={item} style={{ marginBottom: '8px' }}>
                    <a href="#" style={{ color: 'rgba(148, 163, 184, 1)', fontSize: '14px' }}>{item}</a>
                  </li>
                ))}
              </ul>
            </div>
            <div>
              <h5 style={{ marginBottom: '16px' }}>Legal</h5>
              <ul style={{ listStyle: 'none' }}>
                {['Privacy', 'Terms', 'Security', 'Compliance'].map((item) => (
                  <li key={item} style={{ marginBottom: '8px' }}>
                    <a href="#" style={{ color: 'rgba(148, 163, 184, 1)', fontSize: '14px' }}>{item}</a>
                  </li>
                ))}
              </ul>
            </div>
          </div>
          <div style={{
            borderTop: '1px solid rgba(255,255,255,0.05)',
            paddingTop: '40px',
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center',
          }}>
            <div style={{ fontSize: '13px', color: 'rgba(148, 163, 184, 1)' }}>© 2026 Cyzora. All rights reserved.</div>
            <div style={{ display: 'flex', gap: '20px' }}>
              {['GitHub', 'Twitter', 'LinkedIn'].map((social) => (
                <a key={social} href="#" style={{ color: 'rgba(148, 163, 184, 1)', fontSize: '13px' }}>{social}</a>
              ))}
            </div>
          </div>
        </div>
      </footer>
    </>
  );
}